---
title: "Отчёт по лабораторной работе №1"
subtitle: "Установка и конфигурация операционной системы на виртуальную машину"
author: "Александр С. Прядко"
---

# Цель работы

Приобретение практических навыков установки операционной системы на виртуальную машину, настройки минимально необходимых для дальнейшей работы сервисов.

# Задание

1. Создать виртуальную машину с именем `aspryadko`.
2. Установить ОС Rocky Linux 8.10.
3. Собрать информацию о системе (ядро, процессор, память, файловая система).

# Выполнение лабораторной работы

## Установка ОС

В процессе работы была создана виртуальная машина со следующими параметрами:
- **Имя:** aspryadko
- **ОС:** Red Hat (64-bit)
- **Память:** 4096 MB
- **Диск:** 40 GB

При установке было задано имя хоста `aspryadko.localdomain` и создан пользователь `aspryadko` с правами администратора.

## Сбор сведений о системе

1. **Версия ядра Linux.** Использована команда `hostnamectl`.
![Сведения о хосте и ядре](image/host.png)

2. **Модель процессора.** Данные извлечены из `/proc/cpuinfo`.
![Модель процессора](image/cpu.png)

3. **Объем оперативной памяти.** Использована команда `free -m`.
![Оперативная память](image/mem.png)

4. **Тип файловой системы.** Использована команда `df -Th`.
![Файловая система](image/fs.png)

5. **Тип гипервизора.** Использована команда `virt-what`.
![Тип гипервизора](image/virt.png)

# Ответы на контрольные вопросы

1. **Какую информацию содержит учётная запись пользователя?**
   Имя пользователя, зашифрованный пароль, UID (ID пользователя), GID (ID группы), полное имя, домашний каталог и командную оболочку.

2. **Команды терминала:**
   - Справка: `man <команда>`
   - Перемещение: `cd <путь>`
   - Просмотр каталога: `ls`
   - Создание/удаление: `mkdir` / `rm`
   - История: `history`

3. **Что такое файловая система?**
   Это способ организации и хранения данных. Примеры: Ext4, XFS (стандарт в Rocky), NTFS.

4. **Как посмотреть подмонтированные ФС?**
   Команды `mount` или `df -h`.

5. **Как удалить зависший процесс?**
   Команда `kill -9 <PID>`.

# Выводы

Я научился создавать виртуальные машины в VirtualBox, устанавливать Rocky Linux и использовать базовые команды терминала для анализа системы.